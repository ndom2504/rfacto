# Variables d'environnement pour RfactO Backend

# Mode (development ou production)
NODE_ENV=development

# Port du serveur
PORT=4007

# Azure AD Configuration
# Récupérez ces valeurs depuis votre App Registration dans le portail Azure
# https://portal.azure.com → Azure Active Directory → App registrations
AZURE_TENANT_ID=79f19744-dc18-4e15-b6b9-a65e89211776
AZURE_CLIENT_ID=4803f728-e682-4ec9-bb16-bf120a02b237
AZURE_CLIENT_SECRET=e8dcd903-7a4b-400f-8b4b-ceb6eceb1396

# Optionnels (diagnostic auth)
# Audience attendue (Application ID URI) – ex: api://<API_APP_CLIENT_ID>
AZURE_API_AUDIENCE=
# Scopes requis (champ scp du token). Liste séparée par espaces ou virgules.
AZURE_API_REQUIRED_SCOPES=
# Mettre à 1 pour inclure un champ "reason" + détails de debug dans les réponses 401/403
AUTH_DEBUG=0

# Base de données (SQLite par défaut)
DATABASE_URL="file:./rfacto.db"
